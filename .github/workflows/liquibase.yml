name: Liquibase → Supabase (manual first run, no Docker)

on:
  workflow_dispatch: {}   # run manually from Actions tab

jobs:
  migrate:
    runs-on: ubuntu-latest
    env:
      # CHANGE THIS to your Supabase project
      DB_URL: jdbc:postgresql://db.kbqhuywtelqmuyhofjlp.supabase.co:5432/postgres?sslmode=require
      DB_USER: postgres
      DB_PASSWORD: ${{ secrets.DB_PASSWORD }}   # add in Repo → Settings → Secrets → Actions
      CHANGELOG: db/changelog/master.yaml
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Java (required by Liquibase)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Install Liquibase CLI
        shell: bash
        run: |
          set -euo pipefail
          LIQ_VERSION=4.33.0
          INSTALL_ROOT="$(mktemp -d "$RUNNER_TEMP/liquibase-XXXXXX")"
          ZIP="$INSTALL_ROOT/liquibase-${LIQ_VERSION}.zip"
          curl -Ls "https://github.com/liquibase/liquibase/releases/download/v${LIQ_VERSION}/liquibase-${LIQ_VERSION}.zip" -o "$ZIP"
          unzip -q "$ZIP" -d "$INSTALL_ROOT"
          LIQ_HOME="$(find "$INSTALL_ROOT" -mindepth 1 -maxdepth 1 -type d -name 'liquibase*' | head -n1)"
          test -n "$LIQ_HOME" || (echo "Unable to detect Liquibase install folder" && ls -R "$INSTALL_ROOT" && exit 1)
          test -f "$LIQ_HOME/liquibase" || (echo "Liquibase binary not found under $LIQ_HOME" && ls -R "$LIQ_HOME" && exit 1)
          chmod +x "$LIQ_HOME/liquibase"
          echo "$LIQ_HOME" >> "$GITHUB_PATH"
          "$LIQ_HOME/liquibase" --version

      - name: Get PostgreSQL JDBC driver
        shell: bash
        run: |
          set -euo pipefail
          curl -L -o "$RUNNER_TEMP/postgresql.jar" https://jdbc.postgresql.org/download/postgresql-42.7.3.jar
          test -s "$RUNNER_TEMP/postgresql.jar"

      - name: Verify Liquibase is available
        run: |
          which liquibase
          liquibase --version

      - name: Dry run (show first 200 lines)
        working-directory: db
        run: |
          liquibase \
            --classpath="$RUNNER_TEMP/postgresql.jar" \
            --url="${DB_URL}" \
            --username="${DB_USER}" \
            --password="${DB_PASSWORD}" \
            --changeLogFile="${CHANGELOG#db/}" \
            updateSQL | head -n 200 || true

      - name: Apply changelog (update)
        working-directory: db
        run: |
          liquibase \
            --classpath="$RUNNER_TEMP/postgresql.jar" \
            --url="${DB_URL}" \
            --username="${DB_USER}" \
            --password="${DB_PASSWORD}" \
            --changeLogFile="${CHANGELOG#db/}" \
            update
